\# 优化后的提示词（产品规格说明书）



\## 1. 项目概述

开发一个\*\*跨平台桌面辅助工具\*\*（优先支持 Windows，兼顾 macOS/Linux），用于\*\*一键启动 DeepSeek 本地 Web 服务\*\*并内嵌显示界面。

该工具应具备\*\*无感启动、日志可视、托盘常驻、配置持久化\*\*等特性，所有用户数据（含日志、配置）均存储于\*\*软件自身安装目录下的 `config` 文件夹\*\*，不依赖系统注册表或 AppData。



\## 2. 核心功能清单

| 功能模块 | 具体需求 |

|---------|---------|

| \*\*启动服务\*\* | 自动执行 `npx @deepseek-ai/dsh web`，并实时捕获 stdout/stderr 显示在 UI 日志区 |

| \*\*内嵌 Webview\*\* | 服务启动成功后，自动打开内嵌浏览器窗口加载 `http://127.0.0.1:{port}`（默认 3080） |

| \*\*标题栏\*\* | 仅保留最顶部标题栏，显示 `DeepseekHarness`，不包含任何其他按钮（如刷新、返回等） |

| \*\*窗口控制\*\* | 具备最小化、关闭按钮（关闭时隐藏至托盘，不退出进程） |

| \*\*系统托盘\*\* | 右键菜单包含三项：<br> - \*\*打开主界面\*\*（显示/激活主窗口）<br> - \*\*设置\*\*（打开设置窗口）<br> - \*\*退出 Harness\*\*（完全结束进程） |

| \*\*设置界面\*\* | 提供以下可配置项：<br> 1. \*\*服务端口\*\*（输入框，修改后保存并重启服务）<br> 2. \*\*打开日志文件夹\*\*（按钮，打开系统文件管理器定位到 `config/applog`）<br> 3. \*\*打开安装目录\*\*（按钮，打开软件根目录） |

| \*\*首次启动检测\*\* | 检测系统是否安装 Node.js（`node --version`），若未安装：<br> - 弹窗提示用户选择“前往官网下载”或“使用内置 aria2 自动下载安装”<br> - 自动下载需提供进度反馈，并完成静默安装（Windows 下可执行 `.msi` 或 `.exe`） |

| \*\*日志重定向\*\* | 将 `npx` 命令产生的所有输出（含 npm 日志）\*\*同时\*\*写入 `config/npmlog` 目录下的日期滚动文件（如 `npm-YYYY-MM-DD.log`），UI 日志区仅展示最近 200 行 |

| \*\*配置存储\*\* | 所有配置（端口、其他偏好）以 TOML 格式保存在 `config/config.toml`，\*\*禁止\*\*使用 localStorage 或系统 AppData |

| \*\*应用图标\*\* | 使用 `deepseek.ico`（需提供对应尺寸，打包时内嵌） |



\## 3. 技术选型建议（符合“轻量化”原则）

\- \*\*主框架\*\*：Electron（成熟、跨平台、原生托盘支持）或 \*\*Tauri\*\*（更轻量，后端 Rust，但需额外处理 Node.js 检测）；若追求极致轻量且团队熟悉 Rust，可选 Tauri，否则推荐 Electron。

\- \*\*日志处理\*\*：Node.js `fs` 配合 `pino` 或 `winston` 实现文件滚动。

\- \*\*配置读写\*\*：使用 `@iarna/toml` 解析/序列化 TOML。

\- \*\*子进程管理\*\*：`child\\\_process.spawn` 执行 `npx`，并实时监听 `stdout`/`stderr`。

\- \*\*Webview 嵌入\*\*：Electron 的 `<webview>` 标签或 `BrowserView`；Tauri 可使用 `webview` 库。

\- \*\*自动下载 Node.js\*\*：内置 `aria2` 可执行文件（或使用 Node.js 的 `https` 模块下载），并调用系统安装器。



\## 4. 目录结构规划（安装后）

```

安装根目录/

├── deepseek-harness.exe       (主程序)

├── resources/                 (静态资源)

│   └── deepseek.ico

├── config/                    (用户数据目录，程序启动时自动创建)

│   ├── config.toml            (端口等配置)

│   ├── applog/                (UI日志及系统日志)

│   │   └── app-YYYY-MM-DD.log

│   └── npmlog/                (npm子进程原始输出)

│       └── npm-YYYY-MM-DD.log

└── tools/                     (可选，存放aria2等)

\&#x20;   └── aria2c.exe

```



\## 5. 交互流程与UI草图

\### 主窗口

\- 标题栏：自定义无边框，仅显示标题 `DeepseekHarness`，拖拽移动，包含最小化、关闭按钮。

\- 主体：上部为日志显示区域（只读文本框或列表，自动滚动至最新），下部为状态提示（如“服务启动中...”、“服务已就绪”）。

\- 日志区格式：`\\\[时间] \\\[级别] 消息`，颜色区分 info/warn/error。



\### 设置窗口

\- 模态窗口，包含：

&#x20; - 端口输入框 + “保存”按钮（保存后重启服务）。

&#x20; - “打开日志文件夹”按钮。

&#x20; - “打开安装目录”按钮。

\- 设置窗口关闭时自动保存未保存的修改（可加确认）。



\### 托盘菜单

\- 左键单击：显示/隐藏主窗口（若已隐藏则显示，若已显示则聚焦）。

\- 右键菜单：打开主界面、设置、退出。



\## 6. 关键实现细节与边界情况

\### 6.1 Node.js 检测与安装

\- 检测：执行 `node --version`，超时 3 秒，失败则判为未安装。

\- 安装策略：

&#x20; - 选项一：打开官方下载页面（https://nodejs.org）让用户手动安装。



\### 6.2 服务启动与端口变更

\- 启动命令：`npx @deepseek-ai/dsh web --port {port}`（如果该工具支持 `--port`，否则需通过环境变量或配置文件）。

\- 监听服务就绪：通过检测 stdout 输出包含 `listening on` 或尝试轮询 `http://127.0.0.1:{port}` 的 `/health` 端点（若无则简单检查端口可连通）。

\- 端口被占用时，自动尝试下一个可用端口（+1 递增），并在日志中提示；临时端口仅本次运行生效，不写回配置（原端口释放后下次启动仍使用配置端口）。



\### 6.3 日志文件管理

\- 按日期滚动，每天一个文件，保留最近 30 天（可配置）。

\- 日志格式（npmlog）保留原始输出，不做额外加工。

\- applog 记录系统事件（启动、停止、错误等），使用结构化 JSON 或纯文本。



\### 6.4 配置持久化

\- 首次启动创建 `config.toml` 并写入默认端口 `3080`。

\- 修改端口后，停止当前服务，更新配置，重新启动服务。

\- 所有路径使用相对路径（相对于应用根目录），通过 `process.cwd()` 或 `app.getAppPath()` 获取。



\### 6.5 窗口与托盘行为

\- 关闭主窗口（点击 ×）→ 隐藏窗口并显示系统通知（“应用已最小化到托盘”）。

\- 退出 Harness（托盘右键）→ 关闭所有子进程，退出应用。

\- 当服务正在运行时退出，需先 kill 子进程（`child.kill()`）再退出。



\## 7. 打包与发布

\- 使用 `electron-builder` 或 `tauri-builder` 打包，生成安装程序（Windows 下为 NSIS 或 Squirrel）。

\- 安装包需包含 `deepseek.ico` 作为程序图标、文件关联图标。

\- 安装后首次启动自动创建 `config` 目录及默认配置。



\## 8. 异常处理与用户体验

\- 所有异步操作（启动服务、下载 Node.js）提供进度提示（如加载动画）。

\- 子进程崩溃时自动尝试重启（最多 3 次，间隔 5 秒），并在日志中记录。

\- 网络超时、端口冲突等错误以友好弹窗提示，并提供可操作的解决建议。



\## 9. 非功能性要求

\- 内存占用：空闲时 < 50MB，运行时 < 150MB。

\- 启动速度：从双击到显示主窗口 < 3 秒。

\- 日志写入：异步非阻塞，避免影响 UI 响应。



\## 10. 开发环境建议

\- Node.js 18+，pnpm 或 yarn。

\- 使用 TypeScript 增强代码健壮性。

\- 依赖库清单（示例）：

&#x20; - `electron` / `@tauri-apps/api`

&#x20; - `@iarna/toml`

&#x20; - `pino` + `pino-pretty`

&#x20; - `aria2`（可通过 npm 包 `aria2` 或内置二进制）



\---



\*\*最终交付物\*\*：一个符合上述规格的可安装桌面应用，所有源代码及构建脚本。

